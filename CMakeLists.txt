# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.15)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr and automatically regenerate CMakeLists.txt
	include(cmkr.cmake OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS cmake.toml)
endif()

# Variables
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)
set(CAPSTONE_BUILD_SHARED OFF)
set(CAPSTONE_BUILD_STATIC ON)
set(CAPSTONE_BUILD_TESTS OFF)
set(CAPSTONE_BUILD_CSTOOL OFF)
set(CAPSTONE_INSTALL OFF)
set(CAPSTONE_X86_SUPPORT ON)
set(CAPSTONE_ARM_SUPPORT OFF)
set(CAPSTONE_ARM64_SUPPORT OFF)
set(CAPSTONE_MIPS_SUPPORT OFF)
set(CAPSTONE_PPC_SUPPORT OFF)
set(CAPSTONE_SPARC_SUPPORT OFF)
set(CAPSTONE_SYSZ_SUPPORT OFF)
set(CAPSTONE_XCORE_SUPPORT OFF)
set(CAPSTONE_M68K_SUPPORT OFF)
set(CAPSTONE_TMS320C64X_SUPPORT OFF)
set(CAPSTONE_M680X_SUPPORT OFF)
set(CAPSTONE_EVM_SUPPORT OFF)

project(InterObfu
	VERSION
		1.0.0
	DESCRIPTION
		"Intermediate x86 instruction representation for use in obfuscation/deobfuscation"
)

include(FetchContent)

# Fix warnings about DOWNLOAD_EXTRACT_TIMESTAMP
if(POLICY CMP0135)
	cmake_policy(SET CMP0135 NEW)
endif()
message(STATUS "Fetching capstone (5.0.1)...")
FetchContent_Declare(capstone
	GIT_REPOSITORY
		"https://github.com/capstone-engine/capstone.git"
	GIT_TAG
		5.0.1
)
FetchContent_MakeAvailable(capstone)

# Target: interobfu
set(interobfu_SOURCES
	"InterObfu/Assemble.h"
	"InterObfu/Convert.cpp"
	"InterObfu/Convert.h"
	"InterObfu/Instruction.h"
	"InterObfu/Memory.h"
	"InterObfu/Opcode.h"
	"InterObfu/Operand.h"
	"InterObfu/Pattern.h"
	"InterObfu/Peephole.h"
	"InterObfu/Register.h"
	"InterObfu/Segment.h"
	"InterObfu/State.h"
	"InterObfu/ToString.cpp"
	"InterObfu/Utils.h"
	"InterObfu/Value.h"
	"InterObfu/aho-corasick.h"
	cmake.toml
)

add_library(interobfu STATIC)

target_sources(interobfu PRIVATE ${interobfu_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${interobfu_SOURCES})

target_compile_features(interobfu PUBLIC
	cxx_std_17
)

target_include_directories(interobfu PUBLIC
	InterObfu
	"InterObfu/XEDParse"
	"InterObfu/capstone_wrapper"
)

target_link_libraries(interobfu PUBLIC
	capstone
)

set_target_properties(interobfu PROPERTIES
	POSITION_INDEPENDENT_CODE
		ON
)

# Target: interobfu_demo
set(interobfu_demo_SOURCES
	"InterObfu/main.cpp"
	cmake.toml
)

add_executable(interobfu_demo)

target_sources(interobfu_demo PRIVATE ${interobfu_demo_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${interobfu_demo_SOURCES})

target_include_directories(interobfu_demo PRIVATE
	InterObfu
)

target_link_libraries(interobfu_demo PRIVATE
	interobfu
	capstone
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT interobfu_demo)
endif()

# Target: interobfu_tests
set(interobfu_tests_SOURCES
	cmake.toml
	"tests/test_main_simple.cpp"
)

add_executable(interobfu_tests)

target_sources(interobfu_tests PRIVATE ${interobfu_tests_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${interobfu_tests_SOURCES})

target_include_directories(interobfu_tests PRIVATE
	InterObfu
	tests
)

target_link_libraries(interobfu_tests PRIVATE
	interobfu
)

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT interobfu_tests)
endif()
